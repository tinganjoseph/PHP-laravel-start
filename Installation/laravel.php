/*
 todo: Install Laravel Once Composer is installed, you can install Laravel globally:

 *composer global require laravel/installer
 todo: Make sure to add Composer's global bin directory to your PATH:

*echo 'export PATH="$PATH:$HOME/.composer/vendor/bin"' >> ~/.bashrc source ~/.bashrc


*/